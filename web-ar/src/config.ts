// ============================================================
//  config.ts — アプリ全体の設定値をここで管理する
//  位置・スケール・パスなど、触る可能性が高い値をすべてここに集約
// ============================================================

export const CONFIG = {
  // ----------------------------------------------------------------
  // ファイルパス  (public/ 以下への絶対パス)
  // ----------------------------------------------------------------
  modelPath: '/models/character.glb',
  targetPath: '/targets/targets.mind',

  // ----------------------------------------------------------------
  // キャラクター配置  (AR空間内座標 / Three.js単位)
  // ターゲット画像の中心を (0,0,0) とした相対座標
  // ----------------------------------------------------------------
  character: {
    position: [0, 0.08, 0] as [number, number, number], // x, y, z
    scale:    [0.15, 0.15, 0.15] as [number, number, number],
    rotation: [0, 0, 0] as [number, number, number],     // ラジアン
  },

  // ----------------------------------------------------------------
  // 擬似オクルージョン（Selfie Segmentation）
  // ----------------------------------------------------------------
  segmentation: {
    enabled: true,
    // 0 = General model (精度高いが重い), 1 = Landscape model (速い)
    modelSelection: 1 as 0 | 1,
    // 推論解像度 (小さいほど速い、マスクは荒くなる)
    inferenceWidth: 256,
    inferenceHeight: 144,
    // 1fps あたりの推論レート上限（セグメンテーション専用）
    maxFps: 15,
    // 前景マスクの不透明度 (0-1)
    opacity: 0.95,
  },

  // ----------------------------------------------------------------
  // パフォーマンス
  // ----------------------------------------------------------------
  performance: {
    // この FPS を下回ったらオクルージョンを自動OFFにする
    autoDisableOcclusionBelowFps: 15,
    // FPS 計測ウィンドウ（フレーム数）
    fpsSampleWindow: 30,
  },

  // ----------------------------------------------------------------
  // デバッグ / 開発
  // ----------------------------------------------------------------
  debug: false,

  // true にすると targets.mind がなくても動作する
  // キャラが画面中央固定で出続けるデバッグモード
  useFixedAnchor: false,

  // キャラクターの上下ボブ（生きている感）の振幅と速度
  idleBob: {
    amplitude: 0.015,
    speed: 1.5,
  },
} as const;
